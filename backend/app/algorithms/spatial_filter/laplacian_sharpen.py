# 本文件用于实现图像处理算法。
from __future__ import annotations

import cv2
import numpy as np

from app.core.algorithm_framework import build_algorithm_meta, run_standard_algorithm


ALGORITHM_META = build_algorithm_meta("spatial_filter", "laplacian_sharpen")


def run(image: np.ndarray, params: dict | None = None) -> dict:
    """?????????????????????????????????"""
    if image is None:
        raise ValueError("????????")
    if params is None:
        params = {}

    # ??????????????????? ALGORITHM_META ????????
    return run_standard_algorithm(image, params, ALGORITHM_META)
